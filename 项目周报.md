# 项目周报（第 1 周）

## 一、基本信息

- **项目名称：** 多 Agent 协同客诉舆情退赔决策系统（MVP）
- **周报周期：** 2026-08-17 至 2026-08-23
- **周报用途：** 项目日 check、阶段进度评估及下周工作安排
- **统计口径：** 以仓库中的规格文档、代码、测试输出、验证产物、文件时间戳和 Git 提交记录为依据。未提交、未复跑或未形成验证产物的工作，统一标注为「未闭环」，不作为已交付成果。

## 二、本周总体评估

本周完成了从需求对齐、MVP 设计、全栈实现到首轮容器化验证的主线工作。系统已具备工单创建、凭证 OCR、风控与舆情分析、自动退赔、人工审批、审批恢复、角色权限和基础监控等能力。

- **设计：** MVP 主规格、数据模型、接口契约、实施计划及角色化控制台等设计材料已形成。
- **研发：** 后端决策图、Redis Streams Worker、JWT/RBAC、幂等、审批锁、文件上传和 React 前端主体已实现。
- **测试：** 40 项通过、1 项因需要真实 Redis 跳过；S1 人工审批、S2 自动退赔、并发审批和接口幂等均有验证证据。
- **优化：** 压测指标由首测 QPS 24 提升至 191.8，后续记录达到 QPS 381，错误率均为 0%。第二轮结果尚缺 HTML 报告归档。
- **部署：** Docker Compose 已打通 PostgreSQL、Redis、API、Worker、Frontend 五服务；健康检查、自愈和监控栈尚未补齐。

> **重要说明：** 2026-08-19 之后的多项代码和文档改动仍处于工作区未提交状态；OCR 100 张批量验证失败，修复后尚未复跑。下表将「已完成」与「已发现但未闭环」分开记录。

## 三、七日进度明细

### 第 1 天：2026-08-17（周一）

**阶段：需求分析与架构设计**

| 维度 | 当日工作内容 | 进度评估与证据 |
| --- | --- | --- |
| 设计 | 完成需求逆向转述、三方需求互评和边界对齐；输出需求与技术方案、WBS 拆解、架构分析等文档；使用 SpecKit 固化 spec、plan、tasks、data-model、contracts、checklists 等规格材料；确定三态流转、幂等、防资损、人工审批和失败可审计等 P0 红线。 | **已完成。** 证据：`任务一实现/`、`specs/001-refund-decision-mvp/`、`docs/superpowers/specs/` 和 `docs/superpowers/plans/`；Git 提交 `3c819f2`。 |
| 研发 | 按「规格未定稿前不提交业务代码」的约束，本日未开展业务代码实现。 | **符合计划。** 先完成规格，再进入实现阶段。 |
| 测试 | 未执行业务测试；完成验收标准定义，包括挂起恢复、决策边界、并发审批、OCR 置信度路由等。 | **设计完成，执行待后续。** |
| 优化 | 未开展性能或实现优化。 | **符合阶段安排。** |
| 部署 | 未开展部署。 | **符合阶段安排。** |

**当日结论：** 设计基线已建立，后续研发具备统一验收依据。

### 第 2 天：2026-08-18（周二）

**阶段：MVP 全栈研发、核心测试与首次容器化**

| 维度 | 当日工作内容 | 进度评估与证据 |
| --- | --- | --- |
| 设计 | 根据环境限制，将默认 Checkpointer 从 Redis 调整为 PostgreSQL；保留 Redis 配置开关，避免因 RedisJSON 镜像不可用阻塞主链路。 | **已形成可执行方案。** 证据：Loop Engineering 验证报告。 |
| 研发 | 完成 FastAPI 后端、SQLAlchemy 数据模型、JWT/RBAC、幂等键、审批分布式锁、文件存储、LangGraph 决策图、Redis Streams Worker、SSE 接口和 React 前端核心页面；接入 PaddleOCR 本地推理。 | **主体完成。** 证据：提交 `01232ec`、`7e67e05` 等。 |
| 测试 | 修复首轮测试中的 7 个失败和 9 个错误；完成 S1 人工审批、S2 自动退款、并发审批和接口防重测试。S1 最终进入 `COMPLETED/APPROVED`，S2 进入 `AUTO_REFUNDED`；并发审批结果为 `{200: 1, 409: 5}`。 | **核心链路通过。** 证据：`docs/两大核心场景联调证明.md`、E2E 和接口幂等脚本。 |
| 优化 | 针对单 Worker、连接池不足、bcrypt 成本和列表全表扫描进行首轮优化；首轮压测达到 QPS 191.8、P95 260 ms、5735 请求 0 失败；通过数据库条件更新修复并发审批竞态。 | **首轮优化完成。** 证据：`loadtest_report.html`、提交 `10ea9d0`。 |
| 部署 | 使用 Docker Compose 打通 PostgreSQL、Redis、API、Worker、Frontend 五服务；容器内完成建单、决策、挂起、审批和恢复全链路。 | **首次部署验证通过。** 证据：提交 `097b140`、`1fb8f5b` 及 Loop 报告。 |

**当日结论：** MVP 主链路在当天实现并完成首次验证，但登录接口 bcrypt 计算带来较高延迟，且首轮 QPS 处于验收临界值，需要继续优化。

### 第 3 天：2026-08-19（周三）

**阶段：角色化控制台研发与 OCR 批量可靠性验证**

| 维度 | 当日工作内容 | 进度评估与证据 |
| --- | --- | --- |
| 设计 | 设计客服/主管双角色导航、主管待处理优先监控、客服数据隔离、流程总览和数据大屏等控制台能力。 | **设计材料已输出。** 证据：`docs/superpowers/specs/2026-08-19-role-based-refund-console-design.md` 及对应 plan。计划文档的勾选项不等同于已验收。 |
| 研发 | 后端增加按角色过滤的列表和详情接口、`/api/tickets/with-files` multipart 建单接口及中文状态文案；前端增加 `AppShell`、`Monitor`、`MyTickets`、`ProcessOverview`、`StatusLegend` 等页面和组件。 | **功能代码已写入工作区，但未提交。** 需完成审查、提交和回归。 |
| 测试 | 新增角色隔离、multipart 建单、存储键解析等测试；编写 OCR 批量验证脚本，先进行 5 张校准，再执行 100 张样本测试。 | **测试暴露关键问题。** 100 张样本全部未识别出有效文本；50 单保守进入 `SUSPENDED`，50 单因缺少 LLM 凭据进入 `COMPLETED + FAILED`，`error_code=PROCESS_FAILED`。 |
| 优化 | 修复 Worker 解析上传文件路径的问题；为 Docker slim 镜像补充 `libgomp1`、`libgl1`、`libglib2.0-0`，固定 Python 3.11 基线，并补充 LLM 密钥注入通道。 | **修复已写入，但未复跑验证。** OCR 批量可靠性仍未闭环。 |
| 部署 | 仅调整镜像构建配置，本日未重建镜像，未进行修复后的容器回归。 | **未闭环。** |

**当日结论：** 角色化功能主体完成；批量验证证明系统的「宁挂勿错退」兜底有效，但 OCR 和 LLM 依赖链存在问题，必须复跑后才能确认修复效果。

### 第 4 天：2026-08-20（周四）

**阶段：性能与部署口径优化、可靠性方案设计**

| 维度 | 当日工作内容 | 进度评估与证据 |
| --- | --- | --- |
| 设计 | 输出 API 宿主端口迁移至 8001 的规格与计划；输出 OCR 样本可靠性修复计划，明确 multipart 场景、Python 基线、容器内校验和端到端回归任务。 | **设计完成。** 证据：`docs/superpowers/specs/2026-08-20-*` 和对应计划。 |
| 研发 | 统一 Docker API 宿主端口 8001、前端代理端口和 README 命令；补充连接池配置和 PostgreSQL `max_connections=200` 的编排参数。 | **改动已完成，但 08-19 起改动尚未提交。** |
| 测试 | 复测 50 用户、30 秒压测，记录 11303 请求 0 失败、PG 连接拒绝 0 次；在隔离 worktree 中以 TDD 验证 multipart 场景不再传递宿主机绝对路径。 | **已有测试结论；第二轮压测 HTML 未归档。** OCR 可靠性计划任务 2–4 未完成。 |
| 优化 | 定位「4 个 Worker × 每进程 `pool_size=20`」导致 API 独占 80 个连接、接近 PostgreSQL 默认上限的问题；调整为 `pool_size=10 + max_overflow=5`，并提升数据库连接上限。记录 QPS 由 191.8 提升至 381，错误率 0%。 | **优化效果已记录，但缺少第二轮可复核报告。** |
| 部署 | 统一 Docker API 使用 8001；将 PostgreSQL 容量参数纳入 Compose 编排。 | **配置层完成；镜像重建和完整回归尚需补做。** |

**当日结论：** 性能得到明显改善，但应补存第二轮压测原始报告，避免仅以文字结论作为交付证据。

### 第 5 天：2026-08-21（周五）

**阶段：无可核验仓库产出**

| 维度 | 当日记录 | 进度评估 |
| --- | --- | --- |
| 设计 | 未发现新增规格或计划文档。 | 无仓库证据。 |
| 研发 | 未发现 Git 提交或当日文件改动。 | 无仓库证据。 |
| 测试 | 未发现新增测试输出或验证产物。 | 无仓库证据。 |
| 优化 | 未发现新增优化记录。 | 无仓库证据。 |
| 部署 | 未发现新增部署记录。 | 无仓库证据。 |

**当日结论：** 本日报表按「无可核验产出」填写，不对未留痕的线下工作进行追认。如有实际线下工作，应补充会议纪要、截图或提交记录。

### 第 6 天：2026-08-22（周六）

**阶段：无可核验仓库产出**

| 维度 | 当日记录 | 进度评估 |
| --- | --- | --- |
| 设计 | 未发现新增设计产物。 | 无仓库证据。 |
| 研发 | 未发现新增业务代码提交或当日文件改动。 | 无仓库证据。 |
| 测试 | 未发现新增测试运行产物。 | 无仓库证据。 |
| 优化 | 未发现新增优化记录。 | 无仓库证据。 |
| 部署 | 未发现新增部署记录。 | 无仓库证据。 |

**当日结论：** 第 3、4 天暴露的 OCR 批量验证、修复复跑和压测归档问题，本日未见偿还记录，验证债务继续保留。

### 第 7 天：2026-08-23（周日）

**阶段：研发流程工程化收尾**

| 维度 | 当日工作内容 | 进度评估与证据 |
| --- | --- | --- |
| 设计 | 将 SpecKit 规格驱动流程沉淀为 10 个可复用技能，覆盖 constitution、specify、clarify、plan、tasks、analyze、checklist、implement、converge 和 taskstoissues。 | **流程工具层已落盘。** 证据：`.agents/skills/speckit-*/SKILL.md`。尚未形成 Git 提交。 |
| 研发 | 未发现新增业务代码。 | **无业务研发产出。** |
| 测试 | 未发现新增测试；周期末回归记录为 40 passed、1 skipped，用时 7.79 秒。 | **后端核心测试基本通过；真实 Redis 锁测试因环境缺失跳过。** 前端无专门自动化测试文件。 |
| 优化 | 优化研发流程，补齐规格收敛和任务转 issue 环节，针对「计划完成但未执行、代码完成但未提交」的问题完善过程约束。 | **方法论优化完成，尚未验证其对后续交付的实际效果。** |
| 部署 | 未进行新的部署变更，继续沿用五服务编排和 API 8001 端口口径。 | **无新增部署产出。** |

**当日结论：** 完成流程层收尾，但未偿还 OCR 批量验证、故障恢复、监控栈和未提交改动等技术债。

## 四、周度问题与优化建议

### P0：优先处理

1. **提交未闭环改动。** 将 08-19 起的角色化控制台、OCR 修复、端口和编排改动按功能拆分提交；提交前重新执行后端测试，并确认 worktree 中的 multipart 修复是否需要合并。
2. **复跑 OCR 100 张样本。** 使用 `LLM_PROVIDER=mock` 或明确注入测试凭据，重建包含 OCR 系统依赖的镜像，先完成 5 张校准，再复跑 100 张批量验证。目标是清晰样本有有效文本和高置信度，低质量样本按规则转人工，且不出现凭据导致的 `PROCESS_FAILED`。
3. **收口密钥配置。** 检查 `backend/app/config.py` 和 `.env.example`，不得保留真实 API Key 默认值；改为环境变量或密钥管理系统注入，并对已经暴露的 Key 执行吊销和重置。

### P1：补齐交付验收

4. **归档第二轮压测报告。** 生成独立的 `loadtest_report_v2.html`，记录并发数、持续时间、请求量、QPS、P95 和错误率。
5. **补充故障恢复验证。** 为 API、Worker、Frontend 增加 `healthcheck`，执行 `docker kill worker` 场景，记录恢复时延、消息是否重新消费及工单最终状态。
6. **补充监控栈。** 接入 Prometheus 和 Grafana，至少展示 QPS、P95、错误率、工单状态分布和 Worker 消费延迟。
7. **统一文档口径。** README 当前仍有「36 passed」等旧测试口径；应以实际回归结果为准，同时核对技术架构文档中是否存在本仓库没有的组件描述。

### P2：后续提升

8. 为 `GET /api/tickets` 增加分页和必要索引，并单独压测 `/with-files`，获得真实 OCR 路径吞吐数据。
9. 接入 `pytest-cov`，为决策规则、锁和 Worker 核心逻辑设置覆盖率门槛。
10. 完成 OCR 可靠性计划中的容器内校验脚本，并纳入部署后的冒烟检查。

## 五、下周完成标准

| 工作项 | 完成标准 |
| --- | --- |
| 未提交改动 | 代码按功能提交，`git status` 口径清晰，提交内容可追溯。 |
| OCR 可靠性 | 5 张校准和 100 张批量均有产物；清晰样本有效识别，低质量样本不自动退款，凭据配置正确。 |
| 自动化测试 | 后端测试全部可运行；真实 Redis 测试具备环境后补跑；测试数量和 README 口径一致。 |
| 性能验证 | 第二轮 HTML 报告归档，QPS 381 结论可由产物复核。 |
| 容器部署 | 五服务健康检查可用，Worker 被杀后能验证自动恢复和消息处理结果。 |
| 监控 | Prometheus 指标可采集，Grafana 看板可查看核心运行指标。 |

## 六、证据索引

- MVP 规格与计划：`docs/superpowers/specs/2026-08-17-complaint-refund-decision-mvp-design.md`、`docs/superpowers/plans/2026-08-17-complaint-refund-decision-mvp.md`
- Loop 验证报告：`docs/Loop Engineering提示词以及自主验证报告.md`
- 核心场景联调：`docs/两大核心场景联调证明.md`
- OCR 批量产物：`scripts/ocr_suspension_artifacts/ocr-batch-20260819/manifest.json`
- 首轮压测报告：`loadtest_report.html`
- 后端测试：`backend/tests/`
- 容器编排与镜像：`docker-compose.yml`、`backend/Dockerfile`
- 周期内 Git 记录：`3c819f2`、`01232ec`、`7e67e05`、`10ea9d0`、`097b140`、`ce00811` 等

> **真实性声明：** 本周报只记录仓库中能够核验的内容。对于未提交、未复跑、未归档或仅存在于计划中的事项，均明确标注状态，不将计划目标写成已完成结果。
