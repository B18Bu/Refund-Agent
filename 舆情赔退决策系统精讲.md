# 舆情赔退决策系统精讲

> **项目定位：** 面向电商与客服退赔场景的舆情与风险辅助决策 MVP。系统将退款金额、凭证图片及 OCR 识别文本转化为结构化风险信号，通过可解释的规则决定「形成自动退赔决策」或「转人工审批」，并保留工单、审批和节点轨迹。
>
> **答辩关键词：** 人机协同、异步解耦、可解释决策、可恢复工作流、审计留痕。

---

## 1. 项目创作背景

### 1.1 业务痛点

在电商客服退赔场景中，退款并不是单纯的金额判断。客服需要结合用户提交的凭证、退款金额、疑似欺诈风险与舆情风险作出处理；主管则需要对证据不足、高金额或高风险工单进行复核。传统纯人工流程常出现以下问题：

- **处理效率受限：** 大量低风险、小额工单挤占人工审核能力；
- **判断标准不一致：** 不同客服对金额、凭证完整度和风险信号的理解存在差异；
- **风险控制不足：** 恶意退款、薅羊毛或可能升级为舆情事件的工单，不能只按金额快速处理；
- **过程缺乏可追溯性：** 如果没有完整记录「系统为何判断、谁审批、何时恢复」，后续复盘与责任界定会很困难；
- **耗时任务阻塞接口：** OCR 与大模型分析可能耗时或失败，若直接放在建单接口中执行，会影响客服工作台的响应速度。

### 1.2 项目目标

本项目的目标不是替代支付系统，也不是让大模型直接决定资金流向，而是构建一个**退赔辅助决策中台 MVP**：

1. 将凭证识别、风险判断、舆情判断和金额阈值整合为统一决策链路；
2. 让低风险工单快速形成自动退赔决策建议；
3. 让高风险或信息不充分的工单安全地暂停，交由主管审批；
4. 在审批后恢复原工作流，而不是另起一套不连续的处理逻辑；
5. 为工单状态、决策结果、审批意见及节点执行过程提供持久化记录。

> **口径说明：** 当前 `AUTO_REFUNDED` 表示系统已形成并记录自动退赔决策，不代表已对接支付渠道完成真实资金退款。

---

## 2. 方案总览：从建单到闭环处置

### 2.1 核心业务闭环

```text
客服登录并创建工单
        ↓
Redis Streams 投递异步 START 消息
        ↓
Worker 执行 LangGraph 决策工作流
        ↓
凭证 OCR → 欺诈风险分析 → 舆情等级分析 → 规则决策
        ↓
┌─────────────────────────┬────────────────────────┐
│ 低风险且满足阈值         │ 证据不足、金额较高或高风险 │
│ 记录 AUTO_REFUNDED 决策  │ 挂起为 SUSPENDED，等待审批 │
└─────────────────────────┴────────────────────────┘
                                          ↓
                               主管 APPROVE / REJECT
                                          ↓
                          Redis Streams 投递 RESUME 消息
                                          ↓
                          LangGraph 从中断点恢复并落库
```

### 2.2 总体架构

| 层级 | 组件 | 主要职责 |
|---|---|---|
| 展示层 | React、Ant Design、ECharts | 登录、工单工作台、详情、审批面板、流程和运营数据展示 |
| API 层 | FastAPI、Pydantic、JWT | 身份认证、角色鉴权、工单与审批接口、输入校验 |
| 业务编排层 | LangGraph | 定义决策节点、条件路由、人工中断与审批恢复 |
| 异步处理层 | Redis Streams、Worker | 解耦建单接口与 OCR/LLM 耗时计算，消费 START / RESUME 消息 |
| 智能分析层 | PaddleOCR、OpenAI 兼容客户端、DeepSeek / Mock | 提取凭证文本，输出欺诈分和舆情等级 |
| 数据与协调层 | PostgreSQL、Redis | 持久化业务事实、工作流 checkpoint、消息、幂等键、审批锁和事件通知 |
| 部署层 | Docker Compose、nginx | 一键编排前端、API、Worker、PostgreSQL、Redis；提供同源反向代理 |

### 2.3 关键决策规则

自动退赔决策必须同时满足以下条件：

- 退款金额不高于 **300 元**；
- OCR 置信度不低于 **0.6**；
- 欺诈风险分低于 **50**；
- 舆情等级为 **LOW**。

只要任一条件不满足，系统就转入人工审批。当前 MVP 采用保守策略：没有自动拒绝路径，降低模型误判直接造成不当拒赔的风险。

---

## 3. 核心实现拆解

### 3.1 以状态图表达多节点决策工作流

系统使用 LangGraph 编排 `Intake → OCR → Fraud → Sentiment → Decision` 的顺序节点链。决策节点根据规则选择两条明确路径：

- `AUTO_REFUND`：完成自动退赔决策记录；
- `HUMAN_REVIEW`：调用 `interrupt()` 暂停工作流，等待主管指令。

主管审批时，API 先以条件更新抢占 `SUSPENDED` 状态，再投递 `RESUME` 消息；Worker 使用 `Command(resume=...)` 恢复同一个工作流线程。因此，审批前后的业务上下文是一条连续链路。

**答辩表达：** 这里的「多 Agent」准确来说是**基于 LangGraph 编排的多节点 Agent 工作流**。每个节点职责单一、状态共享、路径可追踪；并非多个独立部署并进行自治协商的智能体。

### 3.2 规则与模型分层，避免模型直接决定资金动作

模型负责从 OCR 文本中提取风险信号，例如欺诈分与舆情等级；最终是否自动退赔由纯规则函数控制。这样设计有三层价值：

- **可解释：** 可以准确说明某工单因金额、置信度、风险分或舆情等级中的哪一项不满足而转人工；
- **可测试：** 金额边界、置信度边界和风险阈值可用确定性单元测试覆盖；
- **更安全：** 即使大模型服务异常，系统也会采取保守兜底，而不会让生成式输出直接触发资金动作。

### 3.3 异步化处理，保障客服建单体验

建单接口只负责参数校验、持久化工单和投递 Redis Stream 消息；OCR 与 LLM 调用由独立 Worker 完成。这样避免同步等待图像识别和外部模型响应，使 API 层保持轻量。

Worker 通过消费者组读取消息，并在成功或失败落库后确认消息。工作流异常会记录为 `FAILED / PROCESS_FAILED`，避免任务无状态丢失。

### 3.4 人工审批的并发安全设计

主管审批并不是简单写一条状态，而是采用「Redis 短锁 + 数据库条件更新」的双重保护：

1. Redis 分布式锁防止多个审批请求短时间并发进入；
2. 锁使用随机 Token，并通过 Lua 脚本进行「仅锁持有者可释放」校验；
3. 数据库仅允许将 `SUSPENDED` 原子更新为 `RUNNING`，作为最终业务状态兜底；
4. 成功抢占后写入审批记录，再触发工作流恢复。

这保证了同一工单在并发审批下只会被有效处理一次。

### 3.5 可追溯的数据模型

系统主要持久化四类业务实体：

- `users`：登录用户、密码哈希和角色（客服 `cs`、主管 `sv`）；
- `tickets`：金额、图片路径、OCR 结果、风险、舆情、状态、决策与错误信息；
- `approvals`：主管、审批动作、审批意见和时间；
- `agent_traces`：节点名称、输入输出摘要、执行状态与错误码。

该设计将业务事实放在 PostgreSQL，Redis 主要承担消息、锁、幂等和事件协调，避免把缓存当成最终业务事实来源。

---

## 4. 技术栈选型与替代方案对比

> 下表说明的是当前实现的工程取舍，不等同于对替代技术的绝对优劣判断。选型应服务于本项目的 MVP 范围与实际约束。

| 技术 | 选择原因 | 为什么当前阶段不优先选其他方案 |
|---|---|---|
| **FastAPI** | 原生兼容类型标注、Pydantic 校验与 OpenAPI；适合构建前后端分离的接口服务。 | 相比 Flask，减少手动校验和文档维护；相比 Django，当前不需要模板、Admin 等完整套件，FastAPI 更轻量。 |
| **PostgreSQL + SQLAlchemy** | 工单、用户、审批、轨迹之间关系清晰，适合事务更新与审计查询。 | 相比仅用 Redis，PostgreSQL 更适合作为业务事实来源；相比 MongoDB，当前数据关系固定、关联明确，文档模型优势不突出。 |
| **Redis Streams** | 支持消费者组、待确认消息，适合将 OCR 与 LLM 等长耗时任务移出 API 请求链路。 | 相比同步 HTTP 调用，可降低建单接口阻塞；相比 Kafka、RabbitMQ，Redis 已承担锁和幂等职责，MVP 可减少基础设施种类。 |
| **LangGraph** | 将节点、条件边、中断点和恢复命令显式建模，天然匹配 human-in-the-loop 场景。 | 相比手写状态机，暂停恢复和状态持久化边界更清晰；相比 Celery Canvas，LangGraph 更擅长表达「状态图 + 人工中断」，而 Streams 专注消息运输。 |
| **PostgreSQL Checkpointer** | 当前默认使用 PostgreSQL 保存工作流 checkpoint，复用已有数据库。 | Redis Checkpointer 通常需要 RedisJSON；当前 Compose 使用标准 `redis:7-alpine`，引入 Redis Stack 会增加部署复杂度。 |
| **PaddleOCR** | 可本地识别凭证图片，降低图片发送至外部 OCR SaaS 的数据外发与调用成本。 | 相比云 OCR，可在模型准备后离线运行；代价是镜像更大、运行资源要求更高，适合隐私与可控性优先的 MVP。 |
| **OpenAI 兼容客户端 + DeepSeek / Mock** | 通过兼容协议调用模型，并提供本地确定性 Mock，便于无 API Key 的演示与测试。 | 相比直接绑定某供应商私有 SDK，替换服务端地址的改动更小；但目前尚不是完整的多供应商插件体系。 |
| **确定性规则 + LLM 风险信号** | LLM 处理语义变化，规则控制最终阈值，兼顾灵活性、可解释性与风险边界。 | 相比端到端 LLM 决策，更便于审计和测试；相比纯规则，能吸收 OCR 文本中的语义与关键词信息。 |
| **React + Ant Design + ECharts** | React 适合单页管理端；Ant Design 快速提供表格、表单、描述列表；ECharts 同时覆盖统计图和流程图。 | 相比手写组件和 SVG/Canvas，后台 MVP 开发更快；避免为图表和流程关系引入多套可视化库。 |
| **Docker Compose + nginx** | 将前端、API、Worker、数据库和缓存统一编排，适合本地演示和单机交付。 | 相比 Kubernetes，学习和运维门槛更低；当前规模无需引入容器编排、高可用和自动扩缩容的复杂度。 |

---

## 5. 工程质量与安全控制

### 5.1 已体现的工程实践

- **JWT 鉴权与 RBAC：** 使用 JWT 登录，区分客服与主管角色；审批接口要求主管权限。
- **密码安全：** 密码以 bcrypt 哈希保存，不以明文落库。
- **上传安全：** 限制 JPEG / PNG、检查 MIME 与文件头魔数、使用 UUID 重命名、限制最多 3 张图片。
- **幂等处理：** 通过 Redis `SET NX` 和 24 小时 TTL 抑制重复建单请求。
- **审批并发控制：** Redis 分布式锁叠加数据库条件更新，避免重复审批恢复。
- **失败显式落库：** Worker 异常会写入工单失败状态与错误信息。
- **可测试性：** 决策规则、图流程、认证、锁、幂等和工单接口均有测试覆盖；测试环境通过 Fake OCR、Mock 与内存依赖隔离外部服务。

### 5.2 当前边界与诚实说明

企业级表达必须同时说明已实现能力与下一步建设项：

- 当前系统是**辅助决策 MVP**，尚未接入支付网关、财务流水或真实退款执行；
- 当前前端主建单流程尚未完成「上传凭证后再启动 OCR」的完整闭环；
- 代码包含 SSE 与轮询降级实现，但实时推送链路仍应在完成鉴权与事件监听适配后做端到端验证；
- 当前采用 `Base.metadata.create_all()` 初始化表结构，生产化前应引入 Alembic 等数据库迁移机制；
- Redis Streams 当前未覆盖死信队列、超时回收与完整重试策略；
- 尚未验证高可用、容量上限及 OCR/LLM 的端到端性能指标，因此不应承诺未经压测的数据。

这种边界说明不是弱点，而是体现工程设计中的**事实严谨性、风险意识与演进能力**。

---

## 6. 答辩讲解建议

### 6.1 3 分钟讲解主线

1. **先说痛点：** 客服退赔既要效率，也要避免恶意退款和舆情升级，纯人工慢、纯自动又危险。
2. **再说方案：** 用 OCR 提取凭证文本，用模型生成风险信号，但由确定性规则控制最终自动退赔或人工审批。
3. **强调核心亮点：** LangGraph 将人工审批做成可中断、可恢复的状态机，而不是审批后重跑整条流程。
4. **解释工程保障：** Redis Streams 将耗时任务异步化；PostgreSQL 负责业务事实；Redis 负责消息、锁和幂等；审批过程可追溯。
5. **最后说边界和规划：** 当前是辅助决策 MVP，下一步再接支付、完整上传闭环、消息可靠性治理和生产监控。

### 6.2 可直接使用的项目总结

> 本项目解决的是客服退赔中「效率、风险和可追溯性」难以兼顾的问题。架构上，我将接口服务、异步任务和工作流编排分层：FastAPI 负责受控建单和鉴权，Redis Streams 隔离 OCR 与模型调用，LangGraph 管理多节点决策与人工中断恢复，PostgreSQL 保存工单、审批和轨迹。最关键的设计是模型只输出风险信号，最终动作由可解释规则决定，从而避免大模型直接决定资金相关操作。该项目当前定位为可演示、可测试、可持续演进的退赔辅助决策 MVP。

---

## 7. 后续演进路线

| 优先级 | 演进项 | 目标 |
|---|---|---|
| P0 | 完整凭证上传—启动—OCR 闭环 | 先上传并校验文件，再创建或显式启动工单，消除处理竞态。 |
| P0 | 接入支付与退款编排 | 将「决策结果」与真实支付执行、补偿、对账和资金流水解耦。 |
| P0 | 修复并验证实时状态通道 | 完成 SSE 鉴权与自定义事件监听，保留轮询作为降级策略。 |
| P1 | 消息可靠性治理 | 增加重试次数、超时回收、死信队列、幂等数据库唯一约束与告警。 |
| P1 | 数据库迁移与审计增强 | 引入 Alembic，补充附件元数据、哈希落库和更完整的恢复轨迹。 |
| P1 | 风险策略运营化 | 将阈值配置化，建设人工审批结果回流、规则命中率和误判率评估。 |
| P2 | 生产部署能力 | 增加 TLS、Secret 管理、监控告警、备份恢复、资源限制和容量压测。 |

---

## 8. 代码依据索引

| 主题 | 主要代码位置 |
|---|---|
| LangGraph 节点与工作流 | `backend/app/agents/nodes.py`、`backend/app/agents/graph.py` |
| 自动退赔规则 | `backend/app/agents/decision_rules.py` |
| 模型与 Mock 适配 | `backend/app/agents/llm.py` |
| OCR 实现 | `backend/app/agents/ocr.py` |
| 工单、审批与 SSE 接口 | `backend/app/routers/tickets.py` |
| 文件上传安全控制 | `backend/app/routers/files.py`、`backend/app/storage.py` |
| Redis 消费与工作流恢复 | `backend/app/worker/consumer.py` |
| 锁与幂等 | `backend/app/locks.py`、`backend/app/idempotency.py` |
| 数据模型与数据库配置 | `backend/app/models.py`、`backend/app/db.py` |
| 前端页面与可视化 | `frontend/src/pages/`、`frontend/src/components/FlowCanvas.tsx` |
| 容器化部署 | `docker-compose.yml`、`backend/Dockerfile`、`frontend/Dockerfile`、`frontend/nginx.conf` |

---

**文档版本：** v1.0  
**适用场景：** 项目答辩、技术面试、内部方案讲解  
**事实边界：** 本文以当前代码仓库的静态实现为依据；未将未验证的性能、支付执行或实时推送能力表述为既成事实。
